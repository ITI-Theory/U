numpy>=1.26
scipy>=1.12
# mido and python-osc: pure Python, install normally
mido>=1.3
python-osc>=1.8
# python-rtmidi: NOT needed — midi_input.py uses ctypes/winmm.dll directly
# (python-rtmidi requires a C++ compiler; no pre-built wheel for Python 3.14)
